# Packages with external visibility (in magics or plotting)
awswrangler[redshift]==3.11.0
google-cloud-bigquery[pandas,tqdm]~=3.29  # stormware pins it
matplotlib==3.10.1
# Packages without external visibility (internally used, unlikely to change functionality)
logikal-utils~=1.4
numpy~=2.2
pandas~=2.2
pandas-stubs~=2.2
scipy~=1.15
stormware[google,amazon]~=2.2
xdg-base-dirs~=6.0
